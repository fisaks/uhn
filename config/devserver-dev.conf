# devserver-dev.conf — full simulator mode
MODBUS_SIM=true
IHC_SIM=true
MILIGHT_SIM=true
# ZIGBEE_Z2M — auto-detected from lsusb if not set. Override to force on/off.
ZIGBEE_Z2M=false
ZIGBEE_SIM=true
